package com.example.calculatortest

class Calculator {
    // ini adalah function untuk melakukan penjumlahan 2 data. 2 data tersebut diambil dari 2 parameter, yaitu parameter firt dan second
    // yang sama-sama menggunakan tipe data interger. kemudian function ini akan mengembalikan nilai dari kedua hasil penjumlahan nilai
    // parameter tersebut
    fun add(first: Int, second: Int):Int{
        return first + second;
    }
}